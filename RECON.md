# RECON.md — read-only deep-dives completed ahead of hardening (night shift 2026-07-21; connect added session 2, 07-22)

Common syndrome across ALL foundations: **deploy docs describe the dead AWS home; nothing in any repo knows about Hostinger/Kit 0.** The per-repo "deploy-truth" PR (SUBSTRATE/DEPLOY rewrite + rollback runbook + smoke test) is the repeating core deliverable, fed by kit0-inspect.txt.

## eternitas (#1 — active)
Strong tests (~95 files; PR#101 regression covered: tests/test_auto_demotion.py), no secrets, migrations CI. Gaps: deploy docs 100% AWS (deploy-prod/SUBSTRATE.md, DEPLOY.md — no rollback), deploy.yml + .github/lint refs stale, silent-except paths (FIXED in branch 01), bcrypt rounds=12 hardcoded → slow suite (FIXED in staged branch 02). Suite has deliberate slow sections (rate-limit waits). Known-failing tests pre-bundled as xfail (conftest deny-list, issue #33/PR #71).

## windy-mind (#2 — recon done)
Python 3.12, port 8900, alembic 0001-0006, clean test suite (no xfails), adapters have timeouts + ProviderError wrapping (no silent swallowing; no retries but fallback chain compensates). RED FLAGS: **no CI job runs pytest/ruff at all** (only migrations/lint/deploy) — add test job = PR one; SUBSTRATE.md + CLAUDE.md + deploy.yml pin dead EC2 35.173.154.119 and invent env names that don't exist in code (MIND_HMAC_SECRET, NIM_API_KEY); prod host = flat rsync, not git, hand-edited compose/.env excluded from repo; no rollback anywhere.

## windy-pro / account-server (#3 — recon done)
TS+Express, Node 20, port 8098, SQLite dev/PG prod, jest suite ~60 files w/ good auth coverage; CI test job EXISTS. RED FLAGS: legacy contract-broken Eternitas webhook handler STILL MOUNTED + reachable at POST /api/v1/identity/webhooks/eternitas (identity.ts:1888, HMAC-over-reserialized-JSON at :1906 — can never verify; modern raw-body route is the live one) → remove/410 it = PR one; silent catches in routes/auth.ts (~7 sites, some annotated); DEPLOY.md + deploy-account-server.yml 100% AWS (/opt/windy-pro vs real /root/windy-pro on Kit 0); rollback §520 exists but AWS-keyed.

## windy-connect (#6 — recon done 2026-07-22, via read-only subagent)
TWO stacks: Python 3.11+ Typer CLI (`windy connect`, PyPI, the OIDC Trusted-Publisher reference repo) + TS Cloudflare Worker orchestrator (`backend/`, Durable Object sessions, NOT a separate hidden worker). Flow: device-code (RFC 8628) → magic-link email via Resend → `provisionBundle` fans out Eternitas auto-hatch then Mail/Chat/Mind in parallel → CLI writes per-agent configs (0600). Repo frozen at v0.3.1 (2026-05-21) — predates the Hostinger migration entirely. HEALTH: code ahead of its docs — real JWKS EPT verification, real-mode identity gate, CSRF + rate limiting, reversible writers; suites fully offline-green (≈63 pytest + 28 vitest, NO deny-lists). RED FLAGS: (1) `ENABLE_REAL_PROVISIONING="true"` committed for prod (wrangler.toml:44, deploy.sh:78) while every doc says default false — if any of the 6 Worker secrets is unset, credentialed onboards 500; (2) NO fetch timeouts anywhere except status.ts, and NO compensation on fan-out — a mid-provision failure orphans upstream state (hatched passport / minted Mind key, nothing local); (3) deploy docs stale-to-wrong: dead EC2 IP 35.173.154.119 hardcoded (upstream-gaps.md:67), EC2/Caddy/FastAPI architecture claims for what is actually a CF Worker, backend/README pre-Wave-B (wrong routes, wrong deploy path); (4) dead env vars still shipping (STALWART_ADMIN_* declared+set, read by nothing); (5) `doctor.py::_probe` reports network failures/5xx as ok=True warnings — "All checks passed" while upstreams are down; (6) fabricated `op_unknown_<passport>` operator_id on EPT decode failure (provision.ts:142/228); (7) committed Google OAuth client_id in docs/oauth-setup.md:36 (low severity, client_ids are browser-public; secret properly redacted); (8) bundle "refresh" is re-hatch-as-new-agent, not renewal (needs an Eternitas /renew that doesn't exist). NO .thewindstorm.uk refs. LIVE-VERIFY LIST (can't judge from code): are the 6 Worker prod secrets actually set post-migration; do api.windymind/windymail/chat.windychat resolve to Kit 0; one real end-to-end `windy connect` mint — maps exactly to the standing 05-21 "secrets need re-verify / E2E unproven" note. TOP PRs: reconcile ENABLE_REAL_PROVISIONING + prune dead vars; timeouts + fan-out compensation; deploy-truth rewrite (SUBSTRATE.md for CF-Worker + Kit-0-upstreams topology).

## windy-registry (#7 — recon done 2026-07-22, subagent)
FastAPI + async SQLAlchemy/asyncpg (Postgres+pgvector) + boto3(R2) + Stripe, port 8500, Python ≥3.11 (⚠️ sandbox has 3.10 → CANNOT run its pytest here; code fixes must be Mac-tested). 154 offline tests (in-mem sqlite), real-Postgres CI job, no deny-lists. HEALTH: app code good — uniform outbound timeouts, careful auth/publish/ownership gates, no committed secret values, R2 probe pinned by tests. FINDINGS: (1) Stripe honesty bug — `stripe_connect.py:122-124` sets charges_enabled/payouts_enabled=True unconditionally on OAuth callback without querying Stripe (account_status() exists, unused) → reports payouts Stripe hasn't enabled; (2) weak secret default `STRIPE_STATE_SECRET`→"dev-state-secret-not-for-prod" (`stripe_connect.py:36-38`) = forgeable Connect-OAuth state HMAC if unset in prod; (3) deploy substrate 100% stale AWS — dead EC2 54.88.113.79 everywhere, nightly backup → AWS S3, Kit0/72.60.118.54 in ZERO files; docs claim a pre-rsync snapshot rollback that deploy.yml never creates (`prod-deploy.md:103`); (4) dead `REDIS_URL` config never read though `--workers 2` means per-worker in-mem limiter; (5) webhook secrets stored cleartext in `secret_hash` col (named misleadingly). CRL fails CLOSED in prod → if eternitas CRL down, every authed agent write 503s (coupling to flag). LIVE-VERIFY: prod actually on Kit 0? backups succeeding post-migration (durable-memory risk)? R2 probe green? TOP PRs: Stripe honesty+fail-closed-secret (Mac-test), substrate/docs refresh (doc-only, shippable now), dead-code+config cleanup.

## windy-drops (#8 — recon done 2026-07-22, subagent)
NOT a service — polyglot SDK/schema monorepo (TS npm + Python PyPI), no server/port/deploy. Schema `schemas/windy.drop.v1.json` is SoT, both bindings codegen'd + drift-gated in CI. ~87 tests (node:test + pytest), WD-11 byte-identity gate. HEALTH: clean, minimalist, dual-impl, server-authoritative trust — embodies doctrine well; no fake-success, no retry storms, no live secrets/dead hosts in code. FINDINGS: (1) ⭐ 4 TS fetch calls have NO timeout (`registry.ts:37/59/93`, `withdraw.ts:30`) while Python side already has httpx timeouts — asymmetry; offline-testable w/ existing fetch stubs = clean self-merge (mirror of connect PR); (2) committed throwaway EC test key `tools/conformance/test-keys/test-private.pem` (documented non-real, low sev); (3) withdrawn-probe catch swallows real RegistryError (`publish.ts:69`, `publish.py:73`); (4) TS exit codes string-match "401"/"409" vs Python's proper e.status; (5) phantom docs — `tools/README.md` + `README.md:24` claim R2 helpers that don't exist; EC2 assumptions in DNA_STRAND_MASTER_PLAN. Blast radius confined to authoring/publish (server re-verifies sha256). TOP PRs: TS fetch timeouts (ship now), exit-code-by-status, narrow probe catch, docs cleanup.

## windy-admin (#9 — recon done 2026-07-22, subagent)
FastAPI single service (SPA = vanilla JS served by same app), Python 3.12 (⚠️ sandbox 3.10 → CANNOT run pytest here), SQLite-first, port 8900 (prod 127.0.0.1:8910). ~164 offline tests (ASGITransport), NO CI (billing-locked, confirmed). HEALTH: MOST security-conscious repo of the set — tight CORS/CSP/HSTS, universal httpx timeouts, immutable audit rows, real JWT-forwarding delegation to account-server, honest mailer (no fake-success), loopback+nonce ops-hook. FINDINGS (weighted — holds cross-service tokens): (1) authz — staff JWT decoded with `verify_aud=False` (`auth_admin.py:102`); any same-issuer token with admin_role claim accepted at highest-priv surface, no service/audience binding; (2) `unsubscribe_secret` falls back to static dev HMAC (`mailer.py:32`) → forgeable opt-out tokens if env unset; (3) `campaigns.py:50-63`/`client_config.py:69-73` base64-decode JWT payload WITHOUT sig verify for marketing-suppression targeting (forgeable suppression view, not priv-esc); (4) deploy 100% stale AWS EC2 54.88.113.79, zero Kit0 wiring; (5) personal email hardcoded `alerts.py:26` grantwhitmer3@gmail.com. Secrets in env: eternitas admin email/pw, windy-search admin token, Resend key, ingest tokens. LIVE-VERIFY: admin.windyword.ai served from Kit 0? prod env creds set? account-server JWKS + 16 fleet /version reachable? TOP PRs: bind staff-JWT audience (Mac-test; may need account-server aud stamp — coordinate, frozen), unsubscribe_secret fail-closed (Mac-test), deploy-truth refresh (doc-only, ship now) + reconcile ADR et_plt_* vs actual email/pw auth.

## windy-chat (#10 core — recon done 2026-07-22, subagent; TOP-3 priority per Grant)
Node 22 + Express microservices (10 HTTP services + Synapse Python modules + React/Vite SPA), Matrix/Synapse core, Postgres/Redis/Coturn/nginx. ⭐ Node = TESTABLE in sandbox. ~767 tests (Jest per-service + node:test), offline-runnable (eternitas mock, synapse/LLM stubbed). Deploy docs = CORRECT for Kit 0 (DEPLOY.md + override.yml reference 72.60.118.54 / srv1299061 / 2026-07-15 migration) though stale AWS refs linger in CLAUDE.md/SUBSTRATE/aws-setup. HEALTH: good + self-aware; agent↔human loop thoughtfully hardened (owner-only gate, code-enforced send-confirm, EPT session minting, exponential backoff, revoked-runner pruning). ⭐⭐ TOP BUG (on the grandma core loop): `services/agent-roster/agent-runner.js:263-274` AgentRunner._request() has NO timeout on the Matrix sync/send loop → a half-open socket makes that agent go SILENT until restart (every OTHER outbound call in the service HAS a timeout — only this one bare). SHIPPABLE+TESTABLE. Other findings: port 8106 double-booked (translation + agent-roster); agent Matrix access_tokens stored plaintext at rest in shared SQLite (impersonation risk if leaked); GDPR delete is onboarding-local only (doesn't purge agent_credentials/social/backup/push/directory; account-server webhook only fires if URL overridden from localhost default); static CHAT_API_TOKEN = broad S2S bypass-all of trust gates; mobile OTP endpoints /api/v1/chat/verify/* RETIRED→404 (pro-mobile still calls them — the "dead OTP" note, CONFIRMED). LLM chain (Mind EPT→Anthropic→Groq→stub) all have timeouts; fails honest ("hit a snag"), no fake-success. LIVE-VERIFY: DEPLOY_HOST secret = Kit 0?; docker-compose.prod.yml carries push-bus wiring that lives in un-loaded override.yml?; SYNAPSE_ADMIN_TOKEN/WINDY_JWKS_URL/ETERNITAS_URL set correctly (silent-fail if wrong). TOP PRs: ⭐ _request timeout (ship tonight), fix 8106 collision, complete GDPR fan-out.

## windy-mail (#4 — recon done)
FastAPI control-plane (port 8200) around Stalwart container; Resend smarthost. **The lockbox's "JMAP send broken for ALL accounts" (May 6) appears FIXED in code**: Basic-auth fix, primaryAccounts accountId, two-stage Email/set→EmailSubmission/set, Drafts/Sent self-heal (services/send.py, stalwart_jmap.py) + the 2026-07-06 session-cache-poisoning fix (send.py:294-301, per-account keying); real test coverage exists (test_send_selfheal.py etc.). VERIFY LIVE before closing the lockbox note. RED FLAGS: send.py swallows httpx.ConnectError and returns FAKE message-ids (~594, 634, 747) — send failures masquerade as success (P1 honesty violation in code) = PR one; welcome-email senders catch-all; deploy.yml + DEPLOY.md pin dead EC2 54.88.113.79; no Hostinger runbook, no real rollback.
