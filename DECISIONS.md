# DECISIONS.md — every judgment call, and which principle decided it

Format: date · decision · deciding principle(s) · notes.

- **2026-07-21 · Phase 1 = survey-level, not 58 full builds.** P3 (solve the minimum) + P4 (minimalist). Build verification happens per-repo at hardening time; SOTU marks it TBC honestly.
- **2026-07-21 · Ranked order = identity → mind → accounts → mail → agent.** P6 (Eternitas is load-bearing — "welcome bots" is only safe if the passport office is sound) + P2 (Mind is the portability moat).
- **2026-07-21 · Tier D repos stay parked.** P4 — resurrecting vision scaffolds now is feature bloat.
- **2026-07-21 · Sandbox is network-walled → "one-paste script" pattern.** P1 (Grant stays terminal-free except single pastes) + P5 (dumb script, smart model). Scripts staged in ~/windy, results to ~/windy/.fable/.
- **2026-07-21 · GitHub billing stays broken for now (Grant: no funds, negotiating credit).** Consequence accepted: CI dead, manual deploy runbooks remain the standing path. Revisit the moment his credit lands.
- **2026-07-21 · windy-ops approved by Grant (verbatim yes), staged at ~/windy/windy-ops.** P7.
- **2026-07-21 · Kit 0 snapshot approved by Grant.** Executed via Hostinger API from his terminal. Announced plainly: a new snapshot REPLACES the previous one (Hostinger keeps one).
- **2026-07-21 · Honesty flag: two token values (Brave, PyPI) surfaced in my scratch grep output.** Both were already rotation-flagged in the lockbox; folded into Phase 2 hygiene list. Lesson recorded: always pipe lockbox greps through a redaction sed.
- **2026-07-21 · Grant reports FULL AWS→Hostinger migration (~days ago); lockbox only records partial (account-server 07-16, kernel 07-17).** Verifying with a DNS audit before correcting SOTU — trust but verify, since the lockbox is SoT and currently disagrees with the founder's memory. No AWS teardown assumptions until proven.
- **2026-07-21 · Migration CONFIRMED via DNS; SOTU §1 corrected.** Kit 0 now carries ~everything → it is THE single point of failure. Deciding consequence: Kit 0 backup discipline outranks all repo work before merge eras (P1 honey-badger stability).
- **2026-07-21 · Snapshot reality check.** Hostinger snapshots expire in 24h and there was NO pre-existing snapshot (nothing was overwritten). A snapshot is a merge-era seatbelt, not a backup. Durable backup strategy = open Phase 2 item; check Hostinger auto-backups next terminal run.
- **2026-07-21 · Batch Grant-pastes.** Pending windy-ops pushes + CF origin read + Hostinger backups read + AWS zombie audit ride the NEXT script rather than pinging Grant per item (P1 — minimize founder interrupts).
- **2026-07-21 (night shift) · `.thewindstorm.uk` cleanup CLOSED as already-done.** Evidence-based: every remaining reference across the 7 flagged repos is a deliberate guard/lint blocklist/docs archaeology/comment. Lockbox's 244-ref queue is stale. P4 — no churn PRs against intentional pins.
- **2026-07-21 (night shift) · Eternitas suite slowness root-caused** (bcrypt rounds=12 hardcoded at 4 sites + deliberate rate-limit waits). PR-02 (configurable rounds, test=4) prepared as a patch; NOT applied to the working tree while the Mac-side suite is live-reading it — never mutate a tree mid-test-run.
- **2026-07-21 (night shift) · Script-design lesson: independent steps FIRST, slow steps LAST.** batch3b put a multi-hour suite ahead of Kit 0 inspect + AWS audit, serializing the whole night behind it. Every future batch script front-loads the fast independent reads.
- **2026-07-21 (night shift) · No overnight scheduler.** Considered a scheduled task to self-resume; rejected (P4) — fresh scheduled sessions may lack folder mounts + doctrine context, risk of a confused half-agent. TURNOVER.md + Grant opening a chat is the reliable resume path.
- **2026-07-21 (day) · Eternitas principles-pass verdict: LAUNCH-READY pending merges.** No feature bloat found worth cutting (authenticator/appeals/payments are core to a passport office's accountability mission — P6). Deny-list xfails are honest bookkeeping, not rot. Runbooks now context-refresh-proof (P7).
- **2026-07-21 (day) · Substrate rewrites preserve doctrine sections.** First mind SUBSTRATE draft dropped provider-posture + env notes; caught on self-review and restored corrected (P1 — the runbook is for the next agent, not for brevity awards).
- **2026-07-21 (day) · STAND-DOWN from all Windy Word repos (Grant, mid-flight).** A sibling Fable on Windy 0 is consolidating windy-pro book-launch↔main. Frozen for me: windy-pro (incl. account-server!), windy-pro-mobile, windyword-site, windyword-site-book-launch, windy-word-mcp, windy-fix-me (Word-adjacent — confirm before touching). windy-pro restored to book-launch, clean, my branch deleted (mine to delete). Cascade fix handed off as patch + HANDOFF doc instead of a PR (P6 — agent-to-agent handoff via durable substrate). Ranked list re-slots: after windy-mind close-out → windy-mail (#4), account-server rejoins the queue when consolidation lands.
- **2026-07-21 (day) · PRODUCTION DEPLOY authorized by Grant ("Yes. Deploy.").** Scope: eternitas cf8ff6f + windy-mind ead8073 onto Kit 0, one container each, per-SUBSTRATE runbooks, with recorded-SHA auto-rollback and health gates. Fresh snapshot (12:23 UTC) is the deep net.
- **2026-07-21 (day) · Deploy-script lesson: piping a multi-step script into `ssh 'bash -s'` is unsafe** — docker drained stdin after the first service, silently truncating the run (eternitas deployed, mind never invoked; no partial state, but a real trap). FIX, now standard: every remote command that could read stdin gets `</dev/null`, and multi-service deploys run ONE SERVICE PER SCRIPT. P1 (honey-badger stability of my own tooling).
- **2026-07-21 (day) · eternitas /version reports commit_sha null; windy-mind stamps it correctly.** Not a deploy failure (health + code verified live). Cause: eternitas's host compose doesn't thread COMMIT_SHA build-args. Logged as a small follow-up PR — it matters because deployed-state verification (and the future nightly drift cron) reads /version.
- **2026-07-21 (day) · Mail "send is broken" verdict: STALE, with a caveat.** Evidence: Resend delivery log shows steady successful deliveries through 2026-07-21 incl. Stalwart-generated DMARC reports; Stalwart healthy 4 days; JMAP fixes landed May–July with tests. BUT no API-initiated send in 48h of API logs, so the full path is unproven post-migration. Recorded as superseded-with-caveat rather than "fixed" (P1 — never round "mostly works" up to "works"). Definitive proof = one real send, which is a production action → will announce and ask.
- **2026-07-21 (day) · Test-failure discipline.** windy-mail suite = 603 passed / 1 failed (archive→cloud test). Did NOT assume my change was innocent: batch10 runs that single test on clean main first and refuses to push if it passes there. Verify, don't rationalize.
- **2026-07-21 (day) · Archive-test verdict: STALE TEST, not a product break.** Fake Cloud transport did json.loads() on a multipart upload → every archive looked failed; test had been red long enough to guard nothing. Fixed the test to assert the REAL contract (file part + metadata + service token + owner attribution). Product code untouched. Retention is fine.
- **2026-07-21 (day) · windy-agent first PR = docs/SOUL-MAP.md, not a code fix.** Recon found the repo genuinely well-defended (incl. guards against the exact fake-success class I fixed in mail). The real gap was P7's core artifact: no single map of which files ARE the agent. Wrote it tiered by consequence with verified backup/move commands. P7 + P1 (a grandma surviving a laptop upgrade must be a procedure, not a hope).
- **2026-07-21 (day) · HONESTY CORRECTION on my own claim.** I said the archive-test parser was "verified against a real httpx request before shipping" — it wasn't, fully: my harness pre-read the request, so it missed that multipart bodies are STREAMING (real transports must `await request.aread()`; touching .content raises). The test failed on the Mac, batch11 correctly refused to push, fixed with aread() and re-verified by driving a real AsyncClient THROUGH the transport (the actual code path). Lesson: a verification harness that takes a shortcut the real caller doesn't take is not verification. Say what was actually proven.
- **2026-07-21 (day) · windy-agent: 4 pre-existing test failures found, NOT caused by this era.** (a) 2× `test_sd_notify` — `OSError: AF_UNIX path too long`: macOS temp-dir paths exceed the ~104-char UNIX socket limit. Environmental (would pass on Linux CI); fix = short socket dir under /tmp. (b) 2× `test_phone_provision::TestMockProvisioning` — expects `is_mock=True`, gets `False`. **Flagged as potentially REAL, not just environmental:** if mock mode fails to engage where tests expect it, a test/dev environment could attempt live Twilio provisioning. Not yet root-caused — do NOT assume environmental. Triage before windy-agent is called launch-ready.
- **2026-07-21 (day) · Twilio mock-mode failures root-caused (Grant-approved investigation, kept strictly read-only per his 'Twilio may be unpaid' warning).** Cause: tests don't clear ambient TWILIO_* env; Grant's Mac exports TWILIO_PHONE_NUMBER so provision_phone short-circuits (is_mock=False). Test fix shipped (branch agent-02). REAL SAFETY FINDING: provision_phone() purchases a live number whenever TWILIO_ACCOUNT_SID+AUTH_TOKEN are merely present in env — no explicit spend gate; fires on every hatch. PROPOSED a WINDYFLY_ALLOW_PHONE_PURCHASE opt-in (default off) in PHONE-PURCHASE-PROPOSAL.md — did NOT ship (money-behavior change = Grant's call; consistent with what I told him). P1/P5.
- **2026-07-21 (day) · Mail live-send proof authorized by Grant.** fable-sendtest.sh drives SendService.send() inside the prod api container (as the /send endpoint does) from the standing fable-verify-0705@windymail.ai account → grantwhitmer3@gmail.com, then confirms via Resend log by unique marker. Closes the last mail launch-ready caveat if green. Credentials stay server-side.
- **2026-07-21 (day) · MAIL SEND PROVEN LIVE end-to-end.** Real email fable-verify-0705@windymail.ai → grantwhitmer3@gmail.com landed in Grant's inbox. Server: HTTP 200, message_id "huaaaab0", status "sent" (a real JMAP id, NOT the local-dev fallback removed in PR #96). The lockbox's 2026-05-06 "JMAP send broken for ALL accounts" note is CLOSED. Path proven: HTTP POST /api/v1/send → JMAP → Stalwart → Resend → Gmail.
- **2026-07-21 (day) · Send-test lesson (took 3 tries, told Grant plainly each time).** v1 hand-built AccountService with wrong args; v2 used dependency factories but they need app-lifespan init (get_stalwart raised "not initialized"); v3 correct = call the ALREADY-RUNNING server over HTTP with the account's own token read from the DB. Lesson: to prove a running service works, drive it through its real front door, don't reconstruct its internals in a fresh process. Also: an in-container `python3 -` script is NOT the app — it lacks lifespan singletons.
- **2026-07-21 (day) · Grant asked why I can't run his terminal directly (fair frustration).** Explained: Cowork gives me a NETWORK-WALLED Linux sandbox (github.com reachable; Kit 0/mesh/Hostinger/Cloudflare NOT). His Claude Code terminal reaches them but is a separate tool I can't drive. NEXT-SESSION IMPROVEMENTS to end the paste dance: (1) enable computer-use so I can type into Terminal.app directly; (2) OR install one persistent SSH key reachable from the sandbox to Kit 0. Investigate before continuing Phase 3 — would remove the biggest friction in this whole workflow.
- **2026-07-22 (session 2) · Paste-dance investigation CLOSED — both yesterday's options rejected, third option recommended.** (1) Computer-use: dead end — the platform's own safety tiers make terminals/IDEs click-only (typing and key presses deliberately blocked), so I could never type into Terminal.app; verified from the tool's published access rules, not assumed. (2) Sandbox SSH key to Kit 0: rejected twice over — empirically useless (network re-test 07-22: Kit 0:22 blocked; the wall is the network, not a missing key) AND doctrinally wrong (a standing root key to PRODUCTION would structurally erase the announce-first/one-change/explicit-yes gate; that gate is P1 load-bearing, my convenience doesn't outrank it). (3) RECOMMENDED instead: Grant mints a fine-grained GitHub PAT (sneakyfree repos, contents+PRs read/write) into the lockbox — github.com is the ONE reachable domain, and most paste volume was git push/PR-open/merge chores; a PAT returns those to me while production actions stay human-gated BY DESIGN (that residual paste is the safety gate working, not friction). P1 + P4 + P5. Grant's decision pending; logged before asking.
- **2026-07-22 · Grant chose: token + merge the 5 PRs + connect scout (phone-purchase guard NOT selected — stays pending, do not ship).**
- **2026-07-22 · HONESTY CORRECTION on my own PAT pitch.** I told Grant the token would let me open and merge PRs myself; wrong — PR machinery lives at api.github.com, which stays walled. Corrected to him BEFORE he minted it: PAT buys push/fetch/memory-sync autonomy (the bulk of paste volume); PR-open becomes a link he clicks; merge stays his green button (which IS the authorization gate anyway). Lesson repeated: never round capability up, even in a pitch.
- **2026-07-22 · TOKEN CHAT-PASTE REFUSED; dropoff script pattern established.** Grant, tired and frustrated, offered explicit permission to paste the PAT value into chat ("sandbox environment, confidential, rotate all the time"). Refused — hard rule is no-exceptions, and materially: this transcript is logged/summarized beyond our control, and TOKEN-HYGIENE already classifies chat-pasted values as burned (the Resend keys are on today's rotation list for exactly that setup mistake). Also corrected the premise gently: the token opens the real production codebase. BUILT THE EASY PATH instead of just saying no: `~/windy/drop.sh` — he types one short line in Terminal, pastes the token at a hidden prompt, it lands at `~/windy/.fable/gh-token` (0600; ~/windy verified NOT a git repo, so the file can never ride a commit; value never on screen, never in chat). STANDING PATTERN for all future secret handoffs from Grant. P1 (terminal-free spirit: one tiny step, zero GitHub re-visits) + P5 (dumb script) + hygiene doctrine.
